code = 'import json,os,time
from datetime import datetime

class MusicPDCA:
	def setup(self):
		self.memory_path="memory/music_pdca_cycles.json"
		self.cycles=[]
		if os.path.exists(self.memory_path):
			try:
				data=json.load(open(self.memory_path))
				self.cycles=data.get("cycles